name: tagged_todos_organizer
description: A new Flutter project.
publish_to: 'none' 
version: 1.1.2+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  csv: ^5.0.1
  flutter:
    sdk: flutter
  flutter_colorpicker: ^1.0.3
  flutter_riverpod: ^1.0.4
  image_picker: ^0.8.5+3
  mockito: ^5.2.0
  path: ^1.8.1
  path_provider: ^2.0.11
  sembast: ^3.2.0+1

dev_dependencies:
  build_runner: ^2.2.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
